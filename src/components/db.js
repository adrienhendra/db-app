/* Common DB access */

/* Alias for my console debug */
const Console = console;

export default class DB {
  constructor() {
    Console.log('DB constructed!');
  }

  /* Add common CRUD functions here */
}
